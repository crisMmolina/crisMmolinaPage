# Dominio propio del sitio. Sin esto se usa la URL de Vercel.
# NEXT_PUBLIC_SITIO=https://cristophermolina.com

# Token personal de GitHub de solo lectura (scope read:user).
# Con el, el calendario incluye las contribuciones a repos privados.
# Sin el, solo se muestran las publicas.
# GITHUB_TOKEN=
